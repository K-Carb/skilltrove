# 安全策略

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| 0.1.x | ✅ |

## 报告漏洞

请勿在公开 issue 中提交漏洞。通过 GitHub 私有漏洞报告（Security 标签页 → Report a vulnerability）
或仓库所有者的联系方式私下报告。收到报告后 7 天内回应。

## 安全模型（读我）

SkillTrove 是**单用户本地工具**，安全假设如下：

- 默认只绑定 `127.0.0.1`，写端点（审核/反馈/流水线）**无鉴权**——这是设计决定：
  威胁模型里"本机使用者"是可信方。请勿把端口暴露到公网或不可信的局域网；
  确需远程访问请用 `SKILLTROVE_ALLOWED_HOSTS` 加白 + 反向代理鉴权。
- 内置 DNS rebinding 防护（Host 允许列表）与 `nosniff` 头。
- LLM 调用日志（`data/llm-log.jsonl`）含提示词内容，属本机敏感数据，已被 gitignore。
- 仓库内所有数据均为虚构演示数据（TeamWiki 团队），不含真实凭据。
